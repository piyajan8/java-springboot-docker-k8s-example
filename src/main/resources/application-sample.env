# Sample environment variables for deployment
# Copy this file and customize for your environment

# Application Configuration
SPRING_APPLICATION_NAME=java-springboot-docker-k8s-example
SPRING_PROFILES_ACTIVE=prod
APP_VERSION=1.0.0
APP_ENVIRONMENT=production
DEBUG_ENABLED=false

# Server Configuration
SERVER_PORT=8080
MANAGEMENT_PORT=8080

# Logging Configuration
LOG_LEVEL=INFO

# Health Check Configuration
HEALTH_MEMORY_THRESHOLD=80
HEALTH_DISK_THRESHOLD=75
HEALTH_SHOW_DETAILS=when-authorized
HEALTH_SHOW_COMPONENTS=when-authorized

# Management Endpoints Configuration
MANAGEMENT_ENDPOINTS=health,metrics,info

# External Service Configuration
EXTERNAL_SERVICE_TIMEOUT=10000
EXTERNAL_SERVICE_RETRY_ATTEMPTS=5

# Kubernetes-specific (when using k8s profile)
# These would typically be set by Kubernetes ConfigMaps or Secrets
# KUBERNETES_NAMESPACE=default
# POD_NAME=${HOSTNAME}
# NODE_NAME=${NODE_NAME}